Status: completed

# Add `diagrampilot init` support-file scaffolding

## Parent

- [PRD](../PRD.md)

## What to build

Implement `diagrampilot init` so it creates or updates DiagramPilot support
files only. The command must be low-risk for adoption: it should not scan the
codebase, generate a DiagramPilot Source File, or create Derived Artifacts.

## Acceptance criteria

- [x] `diagrampilot init` completes successfully from a repo root.
- [x] The command creates or updates only support files needed for
      DiagramPilot adoption.
- [x] Tests prove `init` does not scan the repository or generate diagrams by
      default.

## Blocked by

- [01 Bootstrap the TypeScript workspace and `diagrampilot` executable](./01-bootstrap-typescript-workspace-and-cli.md)

## Comments

Implemented 2026-06-02:

- Added `diagrampilot init`.
- The command scaffolds managed DiagramPilot sections in `llms.txt` and
  `docs/diagrampilot.md`.
- Existing local support-file content is preserved, and repeated runs replace
  only the managed DiagramPilot section.
- `init` does not read repository contents or create DiagramPilot Source Files,
  SVG, Mermaid, D2, DOT, or PNG Derived Artifacts by default.

Validation plan:

- Run `npm test`.
- Run `npm run build`.
- From a temporary repository, run:
  `node /home/t3code/projects/DiagramPilot/packages/cli/dist/index.js init`.
- Confirm only `llms.txt` and `docs/diagrampilot.md` are created in that
  temporary repository.

Maintainer approval 2026-06-02:

> *This was generated by AI during triage.*

The implementation was approved after validation. Status remains `completed`.
