# DiagramPilot

> DiagramPilot is a local-first, repo-native diagram compiler for AI coding
> agents. It helps agents check, validate, repair, update, render, and export
> diagrams from structured `*.dp.yaml` and `*.dp.json` source files.

DiagramPilot uses DiagramSpec as its source model. A DiagramPilot source file is
the editable artifact. Mermaid, D2, DOT, SVG, and PNG are derived artifacts, not
the primary source of truth.

Public documentation is hosted under `https://diagrampilot.com`.

## Public Documentation

- [Checkout demo quickstart](https://diagrampilot.com/docs/agents/quickstart.md): Worked demo workflow for repo checks, source repair, SVG refresh, and exports.
- [DiagramSpec](https://diagrampilot.com/docs/agents/spec.md): Structured source model for diagrams.
- [Vercel deployment](https://diagrampilot.com/docs/agents/deployment.md): Static-only Vercel Pro deployment settings and local validation.
- [Error repair](https://diagrampilot.com/docs/agents/error-repair.md): How agents should interpret and fix validation errors.
- [Examples](https://diagrampilot.com/docs/agents/examples.md): Canonical diagram examples.
- [MCP tools](https://diagrampilot.com/docs/agents/mcp.md): Deferred Model Context Protocol plan that will reuse the published docs and schema surface.
- [Prompting guide](https://diagrampilot.com/docs/agents/prompting.md): Recommended instructions for agents using DiagramPilot.

## Current CLI

```bash
diagrampilot init
diagrampilot check
diagrampilot check docs --json
diagrampilot validate docs/architecture.dp.yaml
diagrampilot validate docs/architecture.dp.yaml --json
diagrampilot render docs/architecture.dp.yaml --out docs/architecture.svg
diagrampilot export docs/architecture.dp.yaml --format mermaid
diagrampilot export docs/architecture.dp.yaml --format d2 --out docs/architecture.d2
```

## MVP Boundaries

- `check` is the read-only repo review/CI command.
- `render` produces SVG and requires `--out`.
- Rendered SVG includes deterministic provenance metadata with no wall-clock
  timestamps.
- `export` supports Mermaid and D2.
- `export` prints to stdout by default and writes files with `--out`.
- `validate` checks explicit source files and returns repairable errors.
- `version`, `title`, and `nodes` are required.
- `nodes` must contain at least one node.
- IDs are lowercase snake case and globally unique within a DiagramSpec.
- Icons use namespaced references such as `lucide:database`.
- DOT, PNG, hosted canvas, and project analyzers are deferred.
- MCP is deferred.

## Public Artifacts

- [DiagramSpec v1 JSON Schema](https://diagrampilot.com/schema/diagramspec-v1.schema.json): DiagramSpec v1 JSON Schema is a generated, committed public helper for machine-readable source shape. It does not replace core validation.
- [GitHub repository](https://github.com/StiensWout/DiagramPilot): Source repository once public.
